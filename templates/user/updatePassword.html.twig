{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}
{% block titre %} Modification du mot de passe {% endblock %}
{% block body %}
    <div class="row">
        <div class="col-lg-4 col-md-2 col-1"></div>
        <div class="col-lg-4 col-md-8 col-10">
            <div class="card border-primary" >
                <div class="card-header">Mot de passe</div>
                <div class="card-body">
                    {{ form_start(form) }}
                    {{ form_row(form.password,{
                        "attr":{"placeholder":"Saisir votre mot de passe"},
                        "label":"Mot de Passe"
                    }) }}
                    {{ form_row(form.verificationPassword,{
                        "attr":{"placeholder":"Saisir votre mot de passe"},
                        "label":"Vérification Mot de Passe"
                    }) }}
                    {{ form_widget(form) }}
                    <div class="m-4 text-center">
                        <input class="btn btn-primary m-1" type="submit" value="Valider !">
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
        <div class="col-lg-4 col-md-2 col-1"></div>
    </div>
{% endblock %}



